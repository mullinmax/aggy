# A site that decides by TLS fingerprint refuses a plain Python client
# outright; this lets yt-dlp make the request as a browser instead (see
# IMPERSONATE_TARGET in app.py).
curl_cffi
fastapi
uvicorn
# Extractors break whenever a site changes; this image is meant to be pulled
# and rebuilt often, so the version is deliberately left unpinned.
yt-dlp
